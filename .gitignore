# The FastAPI backend runs locally through Cloudflare Tunnel. It is never
# versioned in this GitHub Pages repository.
/server/

# Data storage and local database files (all data remains private on local machine)
/data/
data/

# Local editor settings
.vscode/

# Local operating-system files
.DS_Store
*.swp
